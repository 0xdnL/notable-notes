name: GitHub Actions Demo
on: [push]

jobs:
#  node-docker:
#    runs-on: ubuntu-latest
#    container:
#      image: node:14.15.0-alpine3.12
#    steps:
#    - name: Log the node version
#      run: |
#        node -v
#        cat /etc/os-release  
  lint-markdown:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/igorshubovych/markdownlint-cli:latest
      #options: -v /__w/notable-notes/notable-notes:/workdir:rw
      #workdir: /workdir
    steps:
    - name: debug output
      run: |
        pwd
        ls -lah
